# Архитектурные решения и безопасность

## Безопасность

### Найденные проблемы
1. **Отсутствие авторизации API (Критичность: High)**
   Эндпоинты `/events` и `/analyze` полностью открыты. Любой, кто знает URL, может читать и писать медицинские данные, просто подставляя `user_id`.
2. **Идентификация пользователя (Критичность: Medium)**
   Бот использует `chat.id` из Telegram напрямую как `user_id`. Нет проверки, зарегистрирован ли пользователь.
3. **CORS и Rate Limiting (Критичность: Low)**
   Отсутствуют. FastAPI поднят без CORSMiddleware и без ограничений на количество запросов.
4. **Шифрование данных (Критичность: Medium)**
   Медицинские данные хранятся в открытом виде в базе данных PostgreSQL/SQLite.
5. **SQL Injection**
   Уязвимостей не найдено. Используется SQLAlchemy ORM, сырые запросы отсутствуют.

### Секреты
Секретов в истории Git не найдено. В файле `.env.example` находятся только заглушки.

## Принятые решения при Handoff
1. **Локализация**: Проект был найден в публичном репозитории GitHub (`VartD/healthos-agent`). Код значительно отличается от заявленного в задании (отсутствуют AI-агенты, Aiogram, React Native).
2. **Ветка handoff**: Создана ветка `manus-handoff-2026-07-10`.
3. **Тестовая БД**: Добавлено правило в `.gitignore` для исключения локальных баз SQLite (`*.db`).
