# Незавершённые задачи (Backlog)

Проект находится на стадии раннего прототипа. До статуса MVP необходимо доказать
надёжность, безопасность, ежедневное использование и пользовательский результат.

## Технический долг и временные решения
- **Миграции БД — выполнено локально**: добавлены Alembic и initial schema migration.
- **Синхронный API (P2)**: текущие sync-маршруты FastAPI выполняются в thread pool и
  для малой нагрузки приемлемы. Переход на async нужен только после измерения нагрузки.
- **Telegram Bot (P1)**: handlers → backend → DB проверены автоматически и в
  реальном Telegram. Осталась единая обработка ошибок и долгосрочная наблюдаемость.
- **Hardcoded правила (P2)**: Все медицинские пороги (глюкоза > 7, мочевая кислота > 360) зашиты прямо в код (`state_engine.py`).
- **Покрытие тестами (P1)**: 26 тестов добавлены; реальный Telegram E2E,
  Docker Compose, persistence и restore drill подтверждены. Остался первый
  GitHub Actions run с PostgreSQL после публикации ветки.
- **Пользовательская авторизация (P0)**: сервисный API-key реализован, но для внешнего
  Mini App необходимы пользовательские сессии и проверка ownership.
- **Наблюдаемость (P0)**: structured logs, health/readiness checks, error tracking,
  uptime alerting. JSON logs и health checks выполнены; error tracking/alerts остаются.
- **Host networking (P1)**: текущая среда Manus требует `network_mode: host`.
  PostgreSQL и backend принудительно слушают `127.0.0.1`; позже нужно вернуть
  переносимую bridge-сеть или выделить отдельный Compose override.
- **Release publication (P0)**: опубликовать ветку, получить зелёный GitHub Actions
  run и развернуть финальный security hardening поверх проверенного RC.

## Запланировано, но не начато (Блокеры для полноценного продукта)
- **Сценарий сна (P0)**: профиль, утренний чекин, недельная сводка и одна NBA
  реализованы. Остались вечерний чекин, reminders и недельный эксперимент.
- **Планировщик (P0)**: утренние/вечерние чекины и напоминания с idempotency и восстановлением пропусков.
- **LLM-оркестратор (P1)**: добавлять после safety-контуров; LLM не заменяет
  детерминированные red flags и не меняет терапию.
- **Telegram Mini App (P1)**: Разработка frontend-части для дашбордов и трендов.
- **Мобильное приложение (P2)**: React Native / Expo клиент.
- **Обработка фото (P1)**: Интеграция Vision API для распознавания показателей с Apple Watch.

## Stage 5 — GPT Parity

- **Свободный текст (выполнен первый срез)**: консервативный русский parser
  распознаёт явные записи воды, еды, кофе/чая, глюкозы, мочевой кислоты,
  давления/пульса, тренировок, сауны, лекарств/добавок и симптомов. При
  неоднозначности ничего не сохраняет и просит уточнение.
- **Safety давления (выполнен первый срез)**: значения ≥180 и/или ≥120 получают
  детерминированное предупреждение, повторное измерение и маршрутизацию к врачу
  или 112 при тревожных симптомах. LLM не участвует в red flags.
- **Подтверждение и сон (выполнено)**: неоднозначные объёмы требуют явного
  подтверждения с TTL 10 минут; полный утренний чекин принимается обычной фразой.
- **Multi-event ввод (выполнено)**: 2–20 событий, разделённых точкой с запятой,
  показываются единым preview и сохраняются одной транзакцией только после «да».
  Нераспознанная часть отклоняет весь набор.
- **Следующий вертикальный срез (P0)**: редактирование отдельных строк preview
  до сохранения и устойчивое хранение ожидающих подтверждений при restart.
- **Фото и OCR/Vision (P1)**: извлечение измерений только через preview и
  подтверждение пользователя до сохранения.
- **Голос (P1)**: транскрипция → тот же parser → подтверждение при неуверенности.
- **Executive Briefing (P1)**: состояние, один главный риск, прогноз и одна NBA;
  сначала детерминированные evidence/safety, затем опциональный LLM-слой.

## Что Codex должен сделать первым
1. **P0 foundation — выполнено локально**: API-key, обязательный `user_id`, валидация,
   исправление latest-value/unit/safety bugs, 15 тестов на этом этапе.
2. **Эксплуатационный контур — принят на deployment host**: migrations, Docker,
   health checks, persistence и restore drill. Остались публикация и GitHub CI.
3. **Первый продуктовый сценарий — частично выполнен**: сон и одна Next Best Action
   реализованы; далее reminders, вечерний чекин и лекарства/добавки.
4. **Только затем AI**: один оркестратор с evidence и safety, а не 14 независимых ролей.
